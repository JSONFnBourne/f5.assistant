# APM (Access Policy Manager)

This directory is for APM-focused material:

- Access policies (per-app and per-session).
- Authentication methods and SSO integration.
- Federation (SAML, OAuth/OIDC).
- Endpoint checks and posture assessments.

