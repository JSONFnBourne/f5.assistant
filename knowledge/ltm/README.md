# LTM (Local Traffic Manager)

This directory is for content focused on LTM:

- Virtual server patterns (HTTP/HTTPS/TCP/UDP).
- Profiles (HTTP, Client/Server SSL, OneConnect, persistence).
- Health monitors and pool design.
- Load-balancing and traffic steering strategies.

Tie in iRules, ASM, APM, and other modules where they interact with LTM.

